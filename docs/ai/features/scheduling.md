# Scheduling / dispatch

- **Owner:** SchedulingService (schema `dispatch`)
- **Purpose:** Appointments; domain includes work orders and payroll entities
- **APIs:** `/api/v1/appointment` (+ health)
- **AuthZ:** Appointment controller currently has no `RequirePermission` attributes
- **Maturity:** Partial
