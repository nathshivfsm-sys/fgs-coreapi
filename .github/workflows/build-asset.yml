# Starts only when Asset Service version changes.
# CI: build/test on PR; push image on merge to dev.
# CD: auto-deploy to EC2 (repository variable EC2_INSTANCE_ID).
name: Build asset

on:
  push:
    branches: [dev]
    paths:
      - "src/AssetService/Fgs.Asset.API/Fgs.Asset.API.csproj"
      - ".github/workflows/build-asset.yml"
  pull_request:
    branches: [dev]
    paths:
      - "src/AssetService/Fgs.Asset.API/Fgs.Asset.API.csproj"
      - ".github/workflows/build-asset.yml"
  workflow_dispatch:
    inputs:
      force:
        description: Build even if <Version> did not change
        type: boolean
        default: false
      push_to_ecr:
        description: Also push image to ECR (normally only on merge to dev)
        type: boolean
        default: false

concurrency:
  group: build-asset-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service: asset
      context: .
      dockerfile: src/Gateway/docker/asset-service.Dockerfile
      ecr_repository: ${{ vars.ECR_REPO || 'fgs/dockers' }}
      version_file: src/AssetService/Fgs.Asset.API/Fgs.Asset.API.csproj
      version_kind: csproj
      test_project: src/AssetService/Fgs.Asset.Tests/Fgs.Asset.Tests.csproj
      force: ${{ github.event_name == 'workflow_dispatch' && inputs.force || false }}
      push_to_ecr: ${{ github.event_name == 'workflow_dispatch' && inputs.push_to_ecr || false }}
      caller_event: ${{ github.event_name }}
    secrets: inherit

  deploy:
    needs: build
    if: needs.build.outputs.image_pushed == 'true'
    uses: ./.github/workflows/reusable-deploy-ec2.yml
    with:
      compose_service: asset-service
      image_tag_prefix: asset
      channel: ${{ needs.build.outputs.channel }}
      github_environment: ${{ needs.build.outputs.github_environment }}
    secrets: inherit
