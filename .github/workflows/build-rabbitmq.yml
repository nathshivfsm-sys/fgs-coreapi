# Starts only when rabbitmq.VERSION changes.
# CI: build on PR; push image on merge to dev.
# CD: auto-deploy to EC2 (repository variable EC2_INSTANCE_ID).
name: Build rabbitmq

on:
  push:
    branches: [dev]
    paths:
      - "src/Gateway/docker/rabbitmq.VERSION"
      - "src/Gateway/docker/rabbitmq.Dockerfile"
      - ".github/workflows/build-rabbitmq.yml"
  pull_request:
    branches: [dev]
    paths:
      - "src/Gateway/docker/rabbitmq.VERSION"
      - "src/Gateway/docker/rabbitmq.Dockerfile"
      - ".github/workflows/build-rabbitmq.yml"
  workflow_dispatch:
    inputs:
      force:
        description: Build even if VERSION did not change
        type: boolean
        default: false
      push_to_ecr:
        description: Also push image to ECR (normally only on merge to dev)
        type: boolean
        default: false

concurrency:
  group: build-rabbitmq-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service: rabbitmq
      context: .
      dockerfile: src/Gateway/docker/rabbitmq.Dockerfile
      ecr_repository: ${{ vars.ECR_REPO || 'fgs/dockers' }}
      version_file: src/Gateway/docker/rabbitmq.VERSION
      version_kind: plain
      test_project: ""
      force: ${{ github.event_name == 'workflow_dispatch' && inputs.force || false }}
      push_to_ecr: ${{ github.event_name == 'workflow_dispatch' && inputs.push_to_ecr || false }}
      caller_event: ${{ github.event_name }}
    secrets: inherit

  deploy:
    needs: build
    if: needs.build.outputs.image_pushed == 'true'
    uses: ./.github/workflows/reusable-deploy-ec2.yml
    with:
      compose_service: rabbitmq
      image_tag_prefix: rabbitmq
      channel: ${{ needs.build.outputs.channel }}
      github_environment: ${{ needs.build.outputs.github_environment }}
    secrets: inherit
