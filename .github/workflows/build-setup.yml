# Starts only when Setup Service version changes.
# CI: build/test on PR; push image on merge to dev|test|main.
# CD: auto-deploy to ECS on GitHub Environment `dev`; `qa`/`prod` wait for approval.
name: Build setup

on:
  push:
    branches: [dev, test, main]
    paths:
      - "src/SetupService/Fgs.Setup.API/Fgs.Setup.API.csproj"
      - ".github/workflows/build-setup.yml"
      - ".github/workflows/reusable-build-service.yml"
      - ".github/workflows/reusable-deploy-ecs.yml"
  pull_request:
    branches: [dev, test, main]
    paths:
      - "src/SetupService/Fgs.Setup.API/Fgs.Setup.API.csproj"
      - ".github/workflows/build-setup.yml"
      - ".github/workflows/reusable-build-service.yml"
  workflow_dispatch:
    inputs:
      force:
        description: Build even if <Version> did not change
        type: boolean
        default: false
      push_to_ecr:
        description: Also push image to ECR (normally only on merge to dev/test/main)
        type: boolean
        default: false

concurrency:
  group: build-setup-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/reusable-build-service.yml
    with:
      service: setup
      context: .
      dockerfile: src/Gateway/docker/setup-service.Dockerfile
      ecr_repository: ${{ vars.ECR_REPO || 'fgs/dockers' }}
      version_file: src/SetupService/Fgs.Setup.API/Fgs.Setup.API.csproj
      version_kind: csproj
      test_project: src/SetupService/Fgs.Setup.Tests/Fgs.Setup.Tests.csproj
      force: ${{ github.event_name == 'workflow_dispatch' && inputs.force || false }}
      push_to_ecr: ${{ github.event_name == 'workflow_dispatch' && inputs.push_to_ecr || false }}
      caller_event: ${{ github.event_name }}
    secrets: inherit

  deploy:
    needs: build
    if: needs.build.outputs.image_pushed == 'true'
    uses: ./.github/workflows/reusable-deploy-ecs.yml
    with:
      ecs_service: setup
      image_tag_prefix: setup
      channel: ${{ needs.build.outputs.channel }}
      github_environment: ${{ needs.build.outputs.github_environment }}
      ecs_cluster: ${{ needs.build.outputs.ecs_cluster }}
    secrets: inherit
