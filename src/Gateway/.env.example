# Local Docker Desktop — copy to src/Gateway/.env (optional; compose has the same develop default).
CREDENTIAL_DISTRIBUTION_KEY=fgs-internal-credential-distribution-key
ASPNETCORE_ENVIRONMENT=Development